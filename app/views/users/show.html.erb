<div class="page-header">
  <h3><%= @user.username %>'s Recommendations</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Photo</th>
        <th>Name</th>
        <th>Address</th>
        <th>Neighborhood</th>
        <th>Actions</th>
      </tr>

      <% @user.recommendations.each do |recommendation| %>
      <tr>
        <td><img src="<%= Restaurant.find_by({:id => recommendation.restaurant_id}).image_url %>" class="img-responsive"</td>
        <td><%= link_to(Restaurant.find_by({:id => recommendation.restaurant_id}).name, Restaurant.find_by({:id => recommendation.restaurant_id}).yelp_url, :target => "_blank") %></td>
        <td><%= link_to(Restaurant.find_by({:id => recommendation.restaurant_id}).address, "https://www.google.com/maps/place/"+Restaurant.find_by({:id => recommendation.restaurant_id}).address.gsub(" ", "+"), :target => "_blank") %></td>
        <!-- <td><%#= Restaurant.find_by({:id => recommendation.restaurant_id}).address %></td> -->
        <td><%= Restaurant.find_by({:id => recommendation.restaurant_id}).neighborhood %></td>
        <td>
          <a href="/recommendations/<%= recommendation.id %>" class="btn btn-primary">Show</a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
